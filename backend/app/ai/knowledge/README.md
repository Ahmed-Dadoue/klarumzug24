This directory contains the active multilingual FAQ knowledge files for Dode.

Active files:
- `faq_de.json`
- `faq_en.json`

Deprecated legacy file:
- `faq.json`

Notes:
- `faq.json` is no longer used by the active AI retrieval flow.
- Keep new FAQ changes in `faq_de.json` and `faq_en.json` only.
- `faq_deprecated.json` is a compatibility backup of the old single-language file.
